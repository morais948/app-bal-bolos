import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {

  },
  cover: {
    width: 140,
    height: 220,
    justifyContent: 'flex-end',
    borderRadius: 8,
    overflow: 'hidden',
    margin: 2
  }
})